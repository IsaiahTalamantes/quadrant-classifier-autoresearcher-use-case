apiVersion: v1 
kind: PersistentVolumeClaim
metadata:
  name: quadrantclasspvc
  namespace: user-irtalama
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn 

  resources:
    requests:
      storage: 5Gi
